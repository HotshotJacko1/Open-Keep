workflows:
  ios-app-build:
    name: iOS App Build
    instance_type: mac_mini_m2
    environment:
      node: 22.22.2
      xcode: 16.4
      cocoapods: default
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.jackbarkerapps.openkeep
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: 'main'
          include: true
          source: true
    integrations:
      app_store_connect: Codemagic
    scripts:
      # pnpm (version pinned by package.json's packageManager, via Corepack) is
      # the only installer that honours pnpm-lock.yaml and the patchedDependencies
      # in pnpm-workspace.yaml -- npm would silently skip the capgo social-login
      # patch and resolve an unpinned dependency set.
      - name: Enable Corepack
        script: corepack enable
      - name: Install dependencies
        script: pnpm install --frozen-lockfile
      - name: Build web application
        script: pnpm build
      - name: Sync Capacitor
        script: pnpm exec cap sync ios
      - name: Update Xcode project signing
        script: xcode-project use-profiles
      - name: Increment build number
        script: |
          cd ios/App
          agvtool new-version -all $BUILD_NUMBER
      - name: Build iOS app
        script: |
          xcode-project build-ipa \
            --project "ios/App/App.xcodeproj" \
            --scheme "App"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - xcodebuild/**/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
